---
phase: 04-deployable-static-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - astro.config.mjs
  - wrangler.toml
  - .cloudflare
autonomous: true
requirements:
  - BLD-01
  - BLD-02
  - BLD-03
  - BLD-04
  - TEST-03
must_haves:
  truths:
    - "bun run build executes bookmark sync before astro build."
    - "The site builds successfully from committed repo contents without the notes directory."
    - "Cloudflare Pages is configured with pinned Bun version and correct build output directory."
    - "Automated verification checks confirm all key routes build without errors."
  artifacts:
    - "`bun run build` chains sync:bookmarks before astro build."
    - "wrangler.toml configures Cloudflare Pages deployment with bun@1.3.11."
    - "`.cloudflare` directory contains pages.json with build settings."
    - "Verification script checks homepage, bookmark detail, tag, and category routes."
  key_links:
    - "`package.json` scripts.build chains `sync:bookmarks` before `astro build`."
    - "`wrangler.toml` configures Pages deployment with correct build command and output directory."
    - "`.cloudflare/pages.json` sets `bun` runtime and `dist` output directory."
---

<objective>
Wire bookmark sync into the build pipeline, configure Cloudflare Pages deployment, and add automated route verification so the site builds and deploys reproducibly without depending on the external notes directory.

Purpose: The project must build and deploy as a self-contained static site that Cloudflare Pages can serve reliably.
Output: Modified build pipeline, Cloudflare Pages configuration, and automated verification checks.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deployable-static-release/04-CONTEXT.md
@package.json
@astro.config.mjs
@scripts/sync-bookmarks.ts
@src/lib/bookmarks/sync.ts
@src/data/bookmarks.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chain bookmark sync into build script</name>
  <files>package.json</files>
  <read_first>package.json, scripts/sync-bookmarks.ts</read_first>
  <action>Modify the `build` script in package.json to run `sync:bookmarks` before `astro build` using Bun's pipeline operator (`&&`). The sync script generates `src/data/bookmarks.json` which Astro then consumes during the build. This ensures the committed bookmark data is fresh before each deployment.</action>
  <verify>bun run build</verify>
  <acceptance_criteria>
    - `bun run build` runs `sync:bookmarks` first, then `astro build`
    - The bookmarks.json is regenerated before Astro reads it
    - Build succeeds when notes directory is unavailable (using committed data)
  </acceptance_criteria>
  <done>bun run build chains sync before build and succeeds without notes directory.</done>
</task>

<task type="auto">
  <name>Task 2: Create wrangler.toml for Cloudflare Pages</name>
  <files>wrangler.toml</files>
  <read_first>package.json, astro.config.mjs</read_first>
  <action>Create `wrangler.toml` in the project root to configure Cloudflare Pages deployment. Set `compatibility_date`, specify the Pages project name, and configure the build output directory as `dist`. The Bun version is controlled by packageManager in package.json which Bun automatically respects during deployment.</action>
  <verify>cat wrangler.toml</verify>
  <acceptance_criteria>
    - wrangler.toml exists and specifies `dist` as the directory
    - Configuration is compatible with Cloudflare Pages deployment
  </acceptance_criteria>
  <done>wrangler.toml configures Cloudflare Pages deployment settings.</done>
</task>

<task type="auto">
  <name>Task 3: Create .cloudflare/pages.json build configuration</name>
  <files>.cloudflare/pages.json</files>
  <read_first>package.json</read_first>
  <action>Create `.cloudflare/pages.json` to explicitly configure the Bun runtime and build settings for Cloudflare Pages. Set the build command to `bun run build` and the output directory to `dist`. This file tells Cloudflare Pages exactly how to build the project and ensures Bun 1.3.11 (from packageManager) is used.</action>
  <verify>cat .cloudflare/pages.json</verify>
  <acceptance_criteria>
    - .cloudflare/pages.json specifies `bun run build` as the build command
    - Output directory is set to `dist`
    - Configuration pins Bun version via packageManager field
  </acceptance_criteria>
  <done>.cloudflare/pages.json configures Bun runtime and build output for Cloudflare Pages.</done>
</task>

<task type="auto">
  <name>Task 4: Add automated route verification script</name>
  <files>scripts/verify-routes.ts</files>
  <read_first>src/data/bookmarks.json, src/pages/index.astro, src/pages/b/[slug].astro, src/pages/tags/[tag].astro, src/pages/category/[...path].astro</read_first>
  <action>Create `scripts/verify-routes.ts` that runs after a build and verifies key routes resolve correctly. The script should: (1) load src/data/bookmarks.json and verify at least one bookmark exists, (2) check that homepage route (/), bookmark detail routes (/b/[slug]), tag routes (/tags/[tag]), and category routes (/category/[...path]) are present in the dist output. Use simple file existence checks since dist is a static build.</action>
  <verify>bun run build && bun run verify:routes</verify>
  <acceptance_criteria>
    - Verification script loads bookmarks.json and extracts route keys
    - Script checks dist/index.html exists (homepage)
    - Script checks dist/b/[slug].html exists for each bookmark
    - Script checks dist/tags/[tag].html exists for each unique tag
    - Script checks dist/category/[...path].html exists for each category path
    - Script exits with code 0 on success, non-zero on failure
  </acceptance_criteria>
  <done>Automated verification confirms all key routes build and resolve successfully.</done>
</task>

<task type="auto">
  <name>Task 5: Add verify:routes script to package.json</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>Add a `verify:routes` script to package.json that runs the verification script. This allows CI to run `bun run verify:routes` after `bun run build` to confirm all routes resolved correctly.</action>
  <verify>bun run verify:routes</verify>
  <acceptance_criteria>
    - `verify:routes` script exists in package.json
    - Running `bun run verify:routes` after build confirms routes
  </acceptance_criteria>
  <done>verify:routes script enables CI route verification.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run build` succeeds
- [ ] Build succeeds even when notes directory is unavailable
- [ ] `wrangler.toml` exists with correct configuration
- [ ] `.cloudflare/pages.json` exists with Bun build settings
- [ ] `bun run verify:routes` confirms homepage, bookmark, tag, and category routes
- [ ] BLD-01, BLD-02, BLD-03, BLD-04, TEST-03 requirements are satisfied
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `bun run build` chains sync:bookmarks before astro build
- Site builds successfully without access to notes directory (using committed bookmarks.json)
- Cloudflare Pages configuration files exist with correct build settings
- Bun version is pinned via packageManager field (bun@1.3.11)
- Automated route verification confirms homepage, bookmark detail, tag, and category routes resolve correctly
- BLD-01, BLD-02, BLD-03, BLD-04, and TEST-03 requirements marked complete in REQUIREMENTS.md
</success_criteria>

<output>
After completion, create `.planning/phases/04-deployable-static-release/04-01-SUMMARY.md`
</output>
