---
phase: 06-tag-selection-fixes-ui-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/index.astro
autonomous: true
requirements:
  - TAG-01
  - TAG-02
  - TAG-03
  - PREVIEW-01
  - CLEANUP-01
must_haves:
  truths:
    - "Selected tag shows visual distinction in dropdown with checkmark indicator."
    - "Preview area displays currently selected tag and clicking it clears the filter."
    - "Hover tooltip shows bookmark description instead of tags."
    - "External icon removed from bookmark title buttons."
  artifacts:
    - "Tag dropdown shows checkmark on selected tag using 'active' class."
    - "Preview area above bookmark list shows 'Selected: [tag]' when active."
    - "Clicking selected tag in preview area resets filter to show all."
    - "Tooltip shows bookmark.description in addition to title and URL."
    - "External SVG icon removed from bookmark-title-btn."
---

<objective>
Fix tag selection interaction bugs (visual distinction and dropdown/preview sync) and remove external link icon per user feedback.

Purpose: Improve tag selection UX with clear visual feedback and remove redundant UI elements.
Output: Fixed tag selection with synced preview area and cleaner bookmark display.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tag-selection-fixes-ui-cleanup/06-CONTEXT.md
@src/pages/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove external icon SVG from bookmark-title-btn</name>
  <files>src/pages/index.astro</files>
  <read_first>src/pages/index.astro</read_first>
  <action>Remove the external link icon SVG element from the bookmark-title-btn button (lines 209-213). The button should only contain the bookmark title text. Keep the button element but remove the SVG child with class "external-icon".</action>
  <verify>grep -A 2 'bookmark-title-btn' src/pages/index.astro | head -10</verify>
  <acceptance_criteria>
    - SVG external-icon element removed from bookmark-title-btn
    - Button still contains bookmark title text
  </acceptance_criteria>
  <done>External icon SVG removed from bookmark titles.</done>
</task>

<task type="auto">
  <name>Task 2: Add visual checkmark indicator for selected tag in dropdown</name>
  <files>src/pages/index.astro</files>
  <read_first>src/pages/index.astro</read_first>
  <action>Update the updateFilterOptions() function where filter option buttons are created. When a tag matches activeTag, add a visual indicator (checkmark SVG or Unicode checkmark) to the button text. Modify line ~324 where className is set to include visual distinction beyond just 'active' class styling. The checkmark should appear as prefix text before the tag name.</action>
  <verify>grep -B 2 -A 8 "filter-option.*active" src/pages/index.astro</verify>
  <acceptance_criteria>
    - Selected tag shows checkmark prefix (✓) in dropdown
    - Unselected tags show no checkmark
  </acceptance_criteria>
  <done>Selected tag shows visual checkmark in dropdown.</done>
</task>

<task type="auto">
  <name>Task 3: Add selected tag preview area above bookmark list</name>
  <files>src/pages/index.astro</files>
  <read_first>src/pages/index.astro</read_first>
  <action>Add a new HTML element above the bookmark list (in the main content area) that displays the currently selected tag. Create a div with class "selected-tag-display" that shows "Selected: [tag name]" when activeTag is set. This element should be clickable and when clicked, it should clear the activeTag (reset filter). Style it with amber accent color similar to tags, with a clear button-like appearance. Add the element near the bookmark list header or filter area.</action>
  <verify>grep -A 3 'selected-tag-display' src/pages/index.astro | head -10</verify>
  <acceptance_criteria>
    - Selected tag preview area visible when filter is active
    - Shows "Selected: [tag]" text
    - Hidden when no tag is selected
    - Clicking clears the filter
  </acceptance_criteria>
  <done>Selected tag preview area added above bookmark list.</done>
</task>

<task type="auto">
  <name>Task 4: Add click handler to selected tag preview to sync with dropdown</name>
  <files>src/pages/index.astro</files>
  <read_first>src/pages/index.astro</read_first>
  <action>Add an event listener to the selected-tag-display element that: (1) clears activeTag when clicked, (2) calls updateFilterOptions() to refresh dropdown UI, (3) calls filterBookmarks() to update the list, (4) clears URL params. This creates bidirectional sync - when user clicks tag in dropdown it shows in preview area, when user clicks tag in preview area it resets dropdown.</action>
  <verify>grep -B 2 -A 5 'selected-tag-display.*addEventListener' src/pages/index.astro</verify>
  <acceptance_criteria>
    - Clicking selected tag clears activeTag
    - Dropdown updates to show no selection
    - Bookmark list shows all items
    - URL params are cleared
  </acceptance_criteria>
  <done>Selected tag preview click handler syncs with dropdown.</done>
</task>

<task type="auto">
  <name>Task 5: Update updateFilterOptions to also render selected tag display</name>
  <files>src/pages/index.astro</files>
  <read_first>src/pages/index.astro</read_first>
  <action>Modify updateFilterOptions() function to also update the selected-tag-display element after updating the filter options. When activeTag is set, show the element with the tag name. When activeTag is empty, hide the element. This ensures the preview area stays in sync with dropdown selection changes.</action>
  <verify>grep -B 2 -A 5 'selected-tag-display' src/pages/index.astro</verify>
  <acceptance_criteria>
    - updateFilterOptions updates selected-tag-display visibility
    - Preview area shows when activeTag is set
    - Preview area hides when activeTag is empty
  </acceptance_criteria>
  <done>updateFilterOptions syncs selected tag display element.</done>
</task>

<task type="auto">
  <name>Task 6: Change tooltip to show description instead of tags</name>
  <files>src/pages/index.astro</files>
  <read_first>src/pages/index.astro</read_first>
  <action>Modify the showPreview() function (around line 244) to change tooltipTags display from showing bookmark.tags to showing bookmark.description. Update tooltipTags element to show the description text instead of tag pills. If description is empty, show a placeholder or hide the description line.</action>
  <verify>grep -B 2 -A 5 'tooltipTags' src/pages/index.astro</verify>
  <acceptance_criteria>
    - Tooltip shows bookmark.description instead of tags
    - Description text is readable in tooltip
    - Empty description handled gracefully
  </acceptance_criteria>
  <done>Tooltip now shows description instead of tags.</done>
</task>

<task type="auto">
  <name>Task 7: Update selected-tag-display CSS styles</name>
  <files>src/pages/index.astro</files>
  <read_first>src/pages/index.astro</read_first>
  <action>Add CSS styles for .selected-tag-display element. Style it as: inline-block with amber text color, cursor pointer, padding, border-radius for rounded look. Add hover state that brightens the amber. Add .hidden class to hide it when no tag is selected. Include styles for the checkmark icon prefix.</action>
  <verify>grep -A 10 '.selected-tag-display' src/pages/index.astro | head -15</verify>
  <acceptance_criteria>
    - Selected tag display styled with amber accent
    - Clear hover state
    - Hidden state when no selection
  </acceptance_criteria>
  <done>CSS styles added for selected tag preview.</done>
</task>

<task type="auto">
  <name>Task 8: Verify build succeeds with all changes</name>
  <files>src/pages/index.astro</files>
  <read_first>src/pages/index.astro</read_first>
  <action>Run the build command to verify all changes compile correctly. Check for any syntax errors or issues. Run: bun run build (or npm run build if bun not configured).</action>
  <verify>bun run build 2>&1</verify>
  <acceptance_criteria>
    - Build completes without errors
    - No TypeScript or Astro errors
  </acceptance_criteria>
  <done>Build succeeds with Phase 6 changes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] External icon SVG removed from bookmark-title-btn (lines 209-213)
- [ ] Selected tag in dropdown shows visual checkmark indicator
- [ ] Selected tag preview area appears above bookmark list when filter is active
- [ ] Clicking selected tag preview clears filter and syncs dropdown
- [ ] Tooltip shows bookmark.description instead of tags on hover
- [ ] CSS styles added for selected-tag-display element
- [ ] Build succeeds with all changes
</verification>

<success_criteria>

- All tasks completed
- TAG-01: Selected tag visually distinct (checkmark) from unselected in dropdown
- TAG-02: Tag selected in dropdown syncs with preview area display
- TAG-03: Tag selected in preview area syncs with dropdown (bidirectional)
- PREVIEW-01: Preview tooltip shows description instead of tags
- CLEANUP-01: External link icon removed from bookmark display
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-tag-selection-fixes-ui-cleanup/06-01-SUMMARY.md`
</output>
