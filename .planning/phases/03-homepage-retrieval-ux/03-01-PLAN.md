---
phase: 03-homepage-retrieval-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/index.astro
  - src/styles/shared.css
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-03
  - ARCH-04
  - ARCH-05
  - ARCH-06
user_setup: []
must_haves:
  truths:
    - "Homepage renders all bookmarks grouped by category, then alphabetically within each category."
    - "Search input filters bookmarks by title, URL, description, and tags with real-time feedback."
    - "Tag and category filters narrow the visible bookmarks via URL query params for shareability."
    - "Empty state displays when no bookmarks match the current search and filter combination."
  artifacts:
    - "Homepage shows all synced bookmarks in a deterministic grouped order."
    - "Search box appears above the bookmark list and filters as the user types."
    - "Tag filter pills and category dropdown appear above the bookmark list."
    - "Empty state message with reset option appears when filters return no results."
  key_links:
    - "`src/pages/index.astro` imports bookmarks from `../data/bookmarks.json` and renders the searchable, filterable list."
    - "`src/styles/shared.css` provides the established design system tokens used by all pages."
---

<objective>
Deliver the public homepage with search, tag/category filters, deterministic ordering, and empty states.

Purpose: Visitors can use the homepage as the fastest retrieval surface for the bookmark archive.
Output: Homepage with full bookmark list, real-time search, tag/category filters, grouped ordering, and empty state.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage-retrieval-ux/03-CONTEXT.md
@prd.md
@src/data/bookmarks.json
@src/lib/bookmarks/types.ts
@src/lib/bookmarks/routes.ts
@src/styles/shared.css
@src/pages/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement homepage bookmark list with grouped ordering</name>
  <files>src/pages/index.astro</files>
  <read_first>src/data/bookmarks.json, src/lib/bookmarks/types.ts, src/lib/bookmarks/routes.ts, src/styles/shared.css, src/pages/tags/[tag].astro, .planning/phases/03-homepage-retrieval-ux/03-CONTEXT.md</read_first>
  <action>Create a homepage bookmark list that displays all bookmarks grouped by category (sorted alphabetically), then alphabetically within each category group. Each bookmark item shows title as a link to `/b/[slug]` and URL as a secondary link. Import `shared.css` for design tokens. Keep the warm cream gradient background, serif fonts, and max-width established in Phase 2.</action>
  <verify>bun run astro build</verify>
  <acceptance_criteria>
    - Homepage renders all bookmarks from `src/data/bookmarks.json`
    - Bookmarks are grouped by categoryPath first, then alphabetical within groups
    - Each item shows title link and URL
    - Page follows the established design system
  </acceptance_criteria>
  <done>Homepage displays all bookmarks in deterministic grouped order.</done>
</task>

<task type="auto">
  <name>Task 2: Add real-time search filtering by title, URL, description, and tags</name>
  <files>src/pages/index.astro</files>
  <read_first>src/styles/shared.css, .planning/phases/03-homepage-retrieval-ux/03-CONTEXT.md</read_first>
  <action>Add a search input above the bookmark list. Implement client-side filtering that searches across title, URL, description, and tags as the user types. Use a debounce of ~150ms to avoid excessive filtering. The search state does not need to persist to URL for this task (handled in Task 3). Display "No bookmarks match your search" when the filter returns zero results.</action>
  <verify>bun run astro build</verify>
  <acceptance_criteria>
    - Search input appears above the bookmark list
    - Typing in search filters bookmarks by title, URL, description, and tags
    - Results update in real-time with debounce
    - Empty state shows when search returns no matches
  </acceptance_criteria>
  <done>Visitors can search bookmarks by title, URL, description, and tags in real-time.</done>
</task>

<task type="auto">
  <name>Task 3: Add tag and category filters with URL query param state</name>
  <files>src/pages/index.astro</files>
  <read_first>src/lib/bookmarks/routes.ts, .planning/phases/03-homepage-retrieval-ux/03-CONTEXT.md</read_first>
  <action>Add tag filter pills and a category dropdown above the bookmark list. Use URL query params (`?tag=foo&category=bar`) so filtered views are shareable. When both search and filters are active, apply them together (AND logic). Sync URL params on change so the page is bookmarkable at any filter combination. Read initial filter state from URL params on page load so direct links work.</action>
  <verify>bun run astro build</verify>
  <acceptance_criteria>
    - Tag filter pills list all unique tags from bookmarks
    - Category dropdown lists all unique category paths
    - Selecting a filter updates the URL query params
    - Page loads correctly when URL has existing filter params
    - Filters combine with search (AND logic)
  </acceptance_criteria>
  <done>Visitors can filter bookmarks by tag and category with shareable URL state.</done>
</task>

<task type="auto">
  <name>Task 4: Add clear empty state when no results match</name>
  <files>src/pages/index.astro</files>
  <read_first>.planning/phases/03-homepage-retrieval-ux/03-CONTEXT.md</read_first>
  <action>Display a friendly empty state when the combination of search and filters returns no bookmarks. The empty state should show a message like "No bookmarks found" with a button or link to reset all filters. Style it consistently with the rest of the design system.</action>
  <verify>bun run astro build</verify>
  <acceptance_criteria>
    - Empty state appears when search + filters return zero results
    - Empty state includes a way to reset filters
    - Empty state is visually distinct but consistent with design system
  </acceptance_criteria>
  <done>Visitors see a clear empty state when no bookmarks match their search and filter combination.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run astro build`
- [ ] Verify homepage renders without errors
- [ ] Verify bookmark list appears with grouped ordering
- [ ] Verify search filters bookmarks in real-time
- [ ] Verify tag and category filters work via URL params
- [ ] Verify empty state appears when no results match
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Homepage lists all synced bookmarks grouped by category, then alphabetically
- Search filters by title, URL, description, and tags with real-time feedback
- Tag and category filters use URL query params for shareability
- Empty state displays when search and filter combination returns no bookmarks
- All pages follow the established design system (serif fonts, warm cream gradient, dark text)
- Route correctness from Phase 2 is preserved (bookmark detail, tag, and category pages still work)
</success_criteria>

<output>
After completion, create `.planning/phases/03-homepage-retrieval-ux/03-01-SUMMARY.md`
</output>
