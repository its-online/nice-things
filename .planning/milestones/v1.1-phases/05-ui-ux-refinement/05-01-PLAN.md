---
phase: 05-ui-ux-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/shared.css
  - src/pages/index.astro
autonomous: true
requirements:
  - THEME-01
  - THEME-02
  - THEME-03
  - LAYOUT-01
  - LAYOUT-02
  - LAYOUT-03
  - LAYOUT-04
  - INTER-01
  - INTER-02
  - INTER-03
  - INTER-04
  - INTER-05
  - SEARCH-01
  - SEARCH-02
  - SEARCH-03
must_haves:
  truths:
    - "Site renders with dark-first design using CSS variables with warm dark palette."
    - "Main bookmark list displays without tag pills; tag/category uses single dropdown with integrated search."
    - "Hovering bookmark shows preview tooltip with title, URL, tags."
    - "Clicking bookmark opens side panel with full details."
    - "Side panel has 'Visit Site' button opening external URL in new tab."
    - "External links have icon + amber color distinction."
    - "Search filters bookmarks in real-time with URL params preserved."
  artifacts:
    - "`src/styles/shared.css` defines dark theme CSS variables (#1a1614 background, amber accents)."
    - "Tag pills removed from bookmark list UI."
    - "Single dropdown combines tag/category selection with integrated search input."
    - "Hover preview tooltip shows bookmark title, URL, tags."
    - "Side panel slides in from right with full bookmark details."
    - "External links show external icon and amber color."
    - "Real-time search with debounce persists to URL query params."
---

<objective>
Redesign the site with a dark-first theme, cleaner layout, improved bookmark interactions (hover preview, side panel), and integrated search with URL persistence.

Purpose: Create a more polished dark-themed interface with intuitive bookmark discovery and detail viewing without page navigation.
Output: Dark-themed homepage with side panel interactions, integrated dropdown search, and real-time filtering.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui-ux-refinement/05-CONTEXT.md
@src/styles/shared.css
@src/pages/index.astro
@src/pages/b/[slug].astro
@src/data/bookmarks.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redefine CSS variables for dark theme</name>
  <files>src/styles/shared.css</files>
  <read_first>src/styles/shared.css</read_first>
  <action>Replace the light CSS variables in shared.css with a warm dark palette. Set: --color-bg to #1a1614, --color-bg-elevated for cards/panels to #242019, --color-bg-input for inputs to #2a241f, --color-text to #e8e0d5 for high contrast, --color-text-muted to rgba(232, 224, 213, 0.6), --color-border to rgba(232, 224, 213, 0.12), --color-accent to #d4a574 (warm amber), --color-accent-hover to #e8b88a, --color-external for external link distinction to #d4a574. Keep --font-serif unchanged. Set html color-scheme to dark.</action>
  <verify>grep -A 20 ':root' src/styles/shared.css</verify>
  <acceptance_criteria>
    - CSS variables use dark palette with #1a1614 background
    - Text uses warm off-white (#e8e0d5) for contrast
    - Amber accent (#d4a574) for interactive elements
    - color-scheme set to dark
  </acceptance_criteria>
  <done>Dark theme CSS variables defined with warm palette.</done>
</task>

<task type="auto">
  <name>Task 2: Update body and link styles for dark theme</name>
  <files>src/styles/shared.css</files>
  <read_first>src/styles/shared.css</read_first>
  <action>Update body background to solid --color-bg instead of gradient. Update a, a:hover styles to use the new color palette with amber accent for hover states. Ensure all contrast ratios meet WCAG AA for body text.</action>
  <verify>cat src/styles/shared.css | head -50</verify>
  <acceptance_criteria>
    - Body uses solid dark background
    - Links use warm off-white text with amber hover
    - All text meets WCAG AA contrast
  </acceptance_criteria>
  <done>Dark theme body and link styles applied.</done>
</task>

<task type="auto">
  <name>Task 3: Redesign index.astro filters section with integrated dropdown</name>
  <files>src/pages/index.astro</files>
  <read_first>src/pages/index.astro</read_first>
  <action>Remove the separate search input and tag-filters div. Create a single combined dropdown/selector component that contains: (1) a search input field at the top for typing queries, (2) tag and category options below that can be selected. The dropdown should appear as a unified control. The HTML structure changes from two separate elements to a single `.filter-select` wrapper containing the input and a `.filter-dropdown` for options. Keep categorySelect and searchInput elements but wrap them in a unified `.filter-control` structure.</action>
  <verify>grep -A 5 'filter-control\|filter-select' src/pages/index.astro</verify>
  <acceptance_criteria>
    - Tag pills removed from HTML
    - Single unified filter control with integrated search input
    - Tag and category selection via dropdown
  </acceptance_criteria>
  <done>Integrated dropdown filter control replaces separate search and tag pills.</done>
</task>

<task type="auto">
  <name>Task 4: Add side panel HTML and styles to index.astro</name>
  <files>src/pages/index.astro</files>
  <read_first>src/pages/index.astro</read_first>
  <action>Add a side panel div after the main element (or within body). The panel should: (1) be hidden by default with transform: translateX(100%), (2) slide in from right when active class is added, (3) contain slots for: bookmark title, external URL link, description, tags list, category path, source path, and a "Visit Site" button, (4) have a close button. The panel width should be ~400px with dark elevated background (#242019). Add the CSS for .side-panel, .side-panel-content, .side-panel-header, .side-panel-close, .side-panel-visit-button, .side-panel-meta, .side-panel-tags.</action>
  <verify>grep -A 3 'side-panel' src/pages/index.astro | head -20</verify>
  <acceptance_criteria>
    - Side panel HTML exists in page structure
    - Panel slides in from right with transform animation
    - Panel has all required content slots
    - Panel styled with elevated dark background
  </acceptance_criteria>
  <done>Side panel structure and styles added to index.astro.</done>
</task>

<task type="auto">
  <name>Task 5: Implement hover preview tooltip functionality</name>
  <files>src/pages/index.astro</files>
  <read_first>src/pages/index.astro</read_first>
  <action>Add a preview tooltip element (.preview-tooltip) that appears on bookmark hover. The tooltip should: (1) be positioned near the hovered bookmark, (2) show bookmark title in bold, (3) show truncated URL, (4) show tag pills (small inline tags). Implement showPreview(bookmark, event) and hidePreview() functions. Add mousemove listener to track position and mouseenter/leave on bookmark items. Debounce tooltip display by 200ms to avoid flicker. Style the tooltip with dark background, amber accent for title, small rounded corners.</action>
  <verify>grep -A 5 'preview-tooltip\|showPreview\|hidePreview' src/pages/index.astro | head -20</verify>
  <acceptance_criteria>
    - Hover shows tooltip after 200ms delay
    - Tooltip displays title, URL, tags
    - Tooltip follows cursor on mousemove
    - Tooltip hides on mouse leave
  </acceptance_criteria>
  <done>Hover preview tooltip implemented for bookmark items.</done>
</task>

<task type="auto">
  <name>Task 6: Implement side panel open/close functionality</name>
  <files>src/pages/index.astro</files>
  <read_first>src/pages/index.astro</read_first>
  <action>Implement openPanel(bookmark) and closePanel() JavaScript functions. When a bookmark is clicked: (1) populate the side panel with bookmark data (title, url, description, tags, categoryPath, sourceRelativePath), (2) add 'active' class to side panel to trigger slide-in animation, (3) prevent event propagation so click doesn't close immediately. The close button should call closePanel() which removes 'active' class. Also close panel when clicking outside (on bookmark-container). Add data-is-external attribute to bookmarks to distinguish external links.</action>
  <verify>grep -A 3 'openPanel\|closePanel' src/pages/index.astro | head -15</verify>
  <acceptance_criteria>
    - Clicking bookmark opens side panel with full details
    - Close button closes panel
    - Clicking outside panel closes it
    - Panel content populated correctly
  </acceptance_criteria>
  <done>Side panel opens with bookmark details on click.</done>
</task>

<task type="auto">
  <name>Task 7: Add external link icon and color styling</name>
  <files>src/pages/index.astro</files>
  <read_first>src/pages/index.astro</read_first>
  <action>Add external link SVG icon next to bookmark titles in the list. All bookmarks have external URLs. Style bookmark titles with amber accent color (#d4a574) and add a small external link icon (SVG: arrow-up-right-from-square). Update renderBookmarks() to: (1) add external-icon span after each bookmark title, (2) add 'external' class to bookmark-item elements. Add CSS for .bookmark-title with amber color and .external-icon styled at 14px with muted opacity.</action>
  <verify>grep -A 3 'external-icon\|bookmark-title' src/pages/index.astro | head -20</verify>
  <acceptance_criteria>
    - All bookmark titles have amber color
    - External icon appears next to each bookmark title
    - Icons are visually subtle (muted opacity)
  </acceptance_criteria>
  <done>External link icons and amber styling applied to bookmarks.</done>
</task>

<task type="auto">
  <name>Task 8: Style side panel "Visit Site" button</name>
  <files>src/pages/index.astro</files>
  <read_first>src/pages/index.astro</read_first>
  <action>Style the .side-panel-visit-button with: amber background (#d4a574), dark text (#1a1614), padding 0.75rem 1.5rem, rounded corners, full width, hover state with lighter amber (#e8b88a). The button should open bookmark.url in a new tab with rel="noopener noreferrer".</action>
  <verify>grep -A 10 'visit-button' src/pages/index.astro | head -15</verify>
  <acceptance_criteria>
    - Visit Site button has amber background
    - Button opens URL in new tab
    - Hover state is visible
  </acceptance_criteria>
  <done>Visit Site button styled with amber accent.</done>
</task>

<task type="auto">
  <name>Task 9: Update bookmark click handler to open side panel instead of navigation</name>
  <files>src/pages/index.astro</files>
  <read_first>src/pages/index.astro</read_first>
  <action>Modify the renderBookmarks function to: (1) change bookmark-item click behavior from navigation to calling openPanel(bookmark), (2) remove href from bookmark-title or make it a button, (3) ensure click on Visit Site button in panel is the only way to navigate to external URL. The bookmark click should NOT navigate to /b/[slug] anymore - it opens the side panel instead.</action>
  <verify>grep -B 2 -A 5 'bookmark-item' src/pages/index.astro | head -25</verify>
  <acceptance_criteria>
    - Clicking bookmark title opens side panel
    - No navigation to /b/[slug] pages from bookmark list
    - Side panel Visit Site button opens external URL
  </acceptance_criteria>
  <done>Bookmark click opens side panel instead of navigating.</done>
</task>

<task type="auto">
  <name>Task 10: Update CSS for dark theme bookmark list styling</name>
  <files>src/pages/index.astro</files>
  <read_first>src/pages/index.astro</read_first>
  <action>Update all bookmark list CSS to use the new dark theme colors: .bookmark-list uses transparent background, .category-heading gets warm muted text color, .bookmark-item uses flex layout with proper spacing, .bookmark-title uses amber accent color with external icon, .bookmark-url uses muted text color. Ensure all hover states work with dark background. Also style .empty-state and .reset-button for dark theme.</action>
  <verify>grep -A 5 '.bookmark-' src/pages/index.astro | head -40</verify>
  <acceptance_criteria>
    - Bookmark list styled for dark theme
    - Category headings use muted warm color
    - Hover states visible on dark background
  </acceptance_criteria>
  <done>Bookmark list CSS updated for dark theme.</done>
</task>

<task type="auto">
  <name>Task 11: Verify all 15 requirements are addressed</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md</read_first>
  <action>Review all 15 v1.1 requirements to ensure each is addressed by the implementation: THEME-01/02/03 (dark theme), LAYOUT-01/02/03/04 (layout changes), INTER-01/02/03/04/05 (bookmark interactions), SEARCH-01/02/03 (search functionality). Check that the implementation matches the specification for each requirement and no requirements are missed or partially implemented.</action>
  <verify>grep -E '^\- \[ \] \*\*(THEME|LAYOUT|INTER|SEARCH)' .planning/REQUIREMENTS.md</verify>
  <acceptance_criteria>
    - THEME-01, THEME-02, THEME-03: Dark theme with CSS variables
    - LAYOUT-01, LAYOUT-02, LAYOUT-03, LAYOUT-04: Layout changes complete
    - INTER-01, INTER-02, INTER-03, INTER-04, INTER-05: Interactions complete
    - SEARCH-01, SEARCH-02, SEARCH-03: Search integration complete
  </acceptance_criteria>
  <done>All 15 v1.1 requirements addressed in implementation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Dark theme CSS variables defined with warm palette (#1a1614 background, #d4a574 amber)
- [ ] Tag pills removed from main UI
- [ ] Single dropdown control combines tag/category selection with search input
- [ ] Hover preview tooltip shows title, URL, tags after 200ms delay
- [ ] Clicking bookmark opens side panel with full details
- [ ] Side panel slides in from right with smooth animation
- [ ] Side panel has "Visit Site" button opening external URL in new tab
- [ ] All bookmark titles have amber color with external icon
- [ ] Real-time search filtering works with URL params preserved
- [ ] `bun run build` succeeds with new implementation
</verification>

<success_criteria>

- All tasks completed
- Dark-first design using CSS variables (#1a1614 background, amber accents)
- Tag pills removed; single dropdown with integrated search
- Hover preview tooltip implemented with title, URL, tags
- Side panel opens on click with full bookmark details
- "Visit Site" button opens external URL in new tab
- External links distinguished with icon + amber color
- Real-time search with URL param persistence
- Build succeeds with new implementation
- THEME-01, THEME-02, THEME-03, LAYOUT-01, LAYOUT-02, LAYOUT-03, LAYOUT-04, INTER-01, INTER-02, INTER-03, INTER-04, INTER-05, SEARCH-01, SEARCH-02, SEARCH-03 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-ux-refinement/05-01-SUMMARY.md`
</output>
