---
phase: 02-static-archive-routes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/b/[slug].astro
  - src/pages/tags/[tag].astro
  - src/pages/category/[...path].astro
  - src/lib/bookmarks/routes.ts
  - src/data/bookmarks.json
autonomous: true
requirements:
  - TAX-01
  - TAX-02
  - TAX-03
  - DET-01
  - DET-02
  - DET-03
  - BLD-05
user_setup: []
must_haves:
  truths:
    - "Every bookmark in `src/data/bookmarks.json` gets a static `/b/[slug]` route at build time."
    - "Every unique tag across all bookmarks gets a static `/tags/[tag]` route at build time."
    - "Every unique category path across all bookmarks gets a static `/category/[...path]` route at build time."
    - "Tag and category pages filter bookmarks to only those assigned to the selected taxonomy."
    - "Bookmark detail pages show title, outbound URL, description when present, tags, category path, and source-relative path."
  artifacts:
    - "`src/pages/b/[slug].astro` renders bookmark detail in a two-column layout (title+description left, metadata sidebar right)."
    - "`src/pages/tags/[tag].astro` renders a vertical list of bookmarks for the given tag."
    - "`src/pages/category/[...path].astro` renders a vertical list of bookmarks for the given category hierarchy."
    - "`src/lib/bookmarks/routes.ts` exports helper functions to extract unique tags and category paths from the bookmark dataset."
  key_links:
    - "`src/pages/b/[slug].astro` imports bookmarks from `../data/bookmarks.json` and renders via `getStaticPaths()`."
    - "`src/pages/tags/[tag].astro` imports bookmarks and uses `getStaticPaths()` to enumerate all unique tags."
    - "`src/pages/category/[...path].astro` imports bookmarks and uses `getStaticPaths()` to enumerate all unique category paths."
---

<objective>
Generate static bookmark detail pages, tag listing pages, and category listing pages from the normalized bookmark dataset.

Purpose: Visitors can navigate to any bookmark, tag, or category directly via static routes without runtime data fetching.
Output: Three new Astro page routes with complete static path generation covering all 186 bookmarks, 105 tags, and 32 categories.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-static-archive-routes/02-CONTEXT.md
@prd.md
@src/data/bookmarks.json
@src/lib/bookmarks/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create route helper library and shared page styles</name>
  <files>src/lib/bookmarks/routes.ts, src/styles/shared.css</files>
  <read_first>src/lib/bookmarks/types.ts, src/data/bookmarks.json, .planning/phases/02-static-archive-routes/02-CONTEXT.md</read_first>
  <action>Create `src/lib/bookmarks/routes.ts` with helper functions: `getAllTags(bookmarks)` returning unique sorted tag array, and `getAllCategoryPaths(bookmarks)` returning unique sorted category path array. Also create `src/styles/shared.css` with the established design tokens: serif font family "Iowan Old Style", "Palatino Linotype", "Book Antiqua", Georgia; warm cream gradient background linear-gradient(180deg, #f6efe0 0%, #fffaf2 100%); dark text #2f2418; max content width 42rem with 1.5rem padding. Add base reset and link styles.</action>
  <verify>bun run astro build --dry-run 2>&1 | head -50</verify>
  <acceptance_criteria>
    - `src/lib/bookmarks/routes.ts` exports `getAllTags` and `getAllCategoryPaths` functions
    - `src/styles/shared.css` contains CSS custom properties for the established design tokens
  </acceptance_criteria>
  <done>Route helpers exist and shared styles define the established design system.</done>
</task>

<task type="auto">
  <name>Task 2: Build bookmark detail page at /b/[slug]</name>
  <files>src/pages/b/[slug].astro</files>
  <read_first>src/data/bookmarks.json, src/lib/bookmarks/types.ts, src/lib/bookmarks/routes.ts, src/styles/shared.css, src/pages/index.astro, .planning/phases/02-static-archive-routes/02-CONTEXT.md</read_first>
  <action>Create `src/pages/b/[slug].astro` with two-column layout: left column shows title as h1 and description when present; right column shows metadata sidebar with outbound URL link, tags as inline chips, category path as breadcrumb, and source-relative path. Use `getStaticPaths()` to enumerate all bookmarks from `src/data/bookmarks.json` and generate one static page per slug. Render outbound URL with proper anchor attributes. Include back-link to homepage at top.</action>
  <verify>bun run astro build</verify>
  <acceptance_criteria>
    - `src/pages/b/[slug].astro` uses `getStaticPaths()` with all 186 bookmark slugs
    - Page renders title, description (when present), outbound URL, tags, category path, and source-relative path
    - Two-column layout: content left, metadata sidebar right
    - All 186 bookmark detail pages build without 404 errors
  </acceptance_criteria>
  <done>Every bookmark has a static `/b/[slug]` route with complete metadata displayed.</done>
</task>

<task type="auto">
  <name>Task 3: Build tag listing page at /tags/[tag]</name>
  <files>src/pages/tags/[tag].astro</files>
  <read_first>src/data/bookmarks.json, src/lib/bookmarks/types.ts, src/lib/bookmarks/routes.ts, src/styles/shared.css, .planning/phases/02-static-archive-routes/02-CONTEXT.md</read_first>
  <action>Create `src/pages/tags/[tag].astro` with simple vertical list layout: each item shows bookmark title and URL in a stack. Use `getStaticPaths()` to enumerate all unique tags via `getAllTags()`. Filter bookmarks to only those where `tags.includes(tag)`. Include tag name as page heading and back-link to homepage.</action>
  <verify>bun run astro build</verify>
  <acceptance_criteria>
    - `src/pages/tags/[tag].astro` uses `getStaticPaths()` with all 105 unique tags
    - Each tag page lists only bookmarks assigned that tag
    - Vertical list shows title and URL stack for each bookmark
    - All 105 tag pages build without 404 errors
  </acceptance_criteria>
  <done>Every tag has a static `/tags/[tag]` route listing only its assigned bookmarks.</done>
</task>

<task type="auto">
  <name>Task 4: Build category listing page at /category/[...path]</name>
  <files>src/pages/category/[...path].astro</files>
  <read_first>src/data/bookmarks.json, src/lib/bookmarks/types.ts, src/lib/bookmarks/routes.ts, src/styles/shared.css, .planning/phases/02-static-archive-routes/02-CONTEXT.md</read_first>
  <action>Create `src/pages/category/[...path].astro` with simple vertical list layout matching the tag pages: title and URL in a stack per bookmark. Use `getStaticPaths()` to enumerate all unique category paths via `getAllCategoryPaths()`. Filter bookmarks to only those where `categoryPath.join('/') === path`. Include category path as page heading (with folder segments as breadcrumb) and back-link to homepage.</action>
  <verify>bun run astro build</verify>
  <acceptance_criteria>
    - `src/pages/category/[...path].astro` uses `getStaticPaths()` with all 32 unique category paths
    - Each category page lists only bookmarks in that category hierarchy
    - Vertical list shows title and URL stack for each bookmark
    - All 32 category pages build without 404 errors
  </acceptance_criteria>
  <done>Every category path has a static `/category/[...path]` route listing only its assigned bookmarks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run astro build`
- [ ] Verify no 404 errors in build output for any generated route
- [ ] Check that build output contains all 186 bookmark pages, 105 tag pages, and 32 category pages
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No missing-page gaps: every bookmark slug, tag, and category path from `src/data/bookmarks.json` has a corresponding static route
- All pages follow the established design system (serif fonts, warm cream gradient, dark text)
- Bookmark detail pages use two-column layout; tag and category pages use vertical list layout
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-archive-routes/02-01-SUMMARY.md`
</output>
