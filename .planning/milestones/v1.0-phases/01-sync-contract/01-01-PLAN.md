---
phase: 01-sync-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - astro.config.mjs
  - tsconfig.json
  - src/env.d.ts
  - src/pages/index.astro
  - src/data/bookmarks.json
  - src/lib/bookmarks/types.ts
  - src/lib/bookmarks/schema.ts
  - src/lib/bookmarks/sync.ts
  - scripts/sync-bookmarks.ts
  - tests/bookmark-sync.test.ts
  - tests/fixtures/bookmarks-source/base/root-bookmark.md
  - tests/fixtures/bookmarks-source/base/tools/test-tool.md
  - tests/fixtures/bookmarks-source/base/articles/ignored-article.md
  - tests/fixtures/bookmarks-source/base/articles/ignored-image.png
  - tests/fixtures/bookmarks-source/base/invalid-frontmatter.md
  - tests/fixtures/bookmarks-source/base/missing-required.md
  - tests/fixtures/bookmarks-source/base/nested/blank-values.md
  - tests/fixtures/bookmarks-source/duplicates/folder-a/same-name.md
  - tests/fixtures/bookmarks-source/duplicates/folder-b/same-name.md
autonomous: true
requirements:
  - SYNC-01
  - SYNC-02
  - SYNC-03
  - SYNC-04
  - SYNC-05
  - SYNC-06
  - SYNC-07
  - SYNC-08
  - SYNC-09
  - SYNC-10
  - TEST-01
  - TEST-02
user_setup: []
must_haves:
  truths:
    - "`bun run sync:bookmarks` reads the notes corpus, skips excluded or invalid records with warnings, and writes a committed normalized dataset to `src/data/bookmarks.json`."
    - "Synced records always expose normalized tags, normalized done state, derived categoryPath, filename-based slug, and sourceRelativePath."
    - "Duplicate filename slugs abort sync with a clear error before output is written."
    - "`bun test` covers normalization edge cases, skip/warn behavior, excluded folders, and duplicate detection."
  artifacts:
    - "Astro/Bun project scaffold exists with package scripts for sync and tests."
    - "`src/lib/bookmarks/sync.ts` contains the reusable sync pipeline."
    - "`scripts/sync-bookmarks.ts` provides the CLI entrypoint."
    - "`tests/bookmark-sync.test.ts` and fixture files exercise the required edge cases."
    - "`src/data/bookmarks.json` contains generated bookmark data from the live notes source."
  key_links:
    - "`package.json` exposes `sync:bookmarks` and `test` scripts that call the implemented sync/test code."
    - "`scripts/sync-bookmarks.ts` imports the sync pipeline from `src/lib/bookmarks/sync.ts`."
    - "`tests/bookmark-sync.test.ts` verifies the same sync pipeline used by the CLI."
---

<objective>
Create the repository foundation and bookmark sync contract for Phase 1.

Purpose: establish one committed normalized bookmark artifact that later Astro phases can build on without re-deciding ingestion behavior.
Output: an Astro/Bun scaffold, reusable sync pipeline, generated `src/data/bookmarks.json`, and automated sync tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-sync-contract/01-CONTEXT.md
@prd.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the Astro/Bun project and shared bookmark contract</name>
  <files>package.json, astro.config.mjs, tsconfig.json, src/env.d.ts, src/pages/index.astro, src/lib/bookmarks/types.ts, src/lib/bookmarks/schema.ts, src/data/bookmarks.json, .gitignore</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-sync-contract/01-CONTEXT.md, prd.md, .gitignore</read_first>
  <action>Create a minimal Astro project foundation in the existing repo without overwriting planning files. Add `package.json` with Bun-first scripts: `dev`, `build`, `preview`, `sync:bookmarks`, and `test`. Install the minimum dependencies needed for this phase: `astro`, `gray-matter`, `zod`, `typescript`, and `@types/bun`. Add `astro.config.mjs`, `tsconfig.json`, and `src/env.d.ts` so Astro and TypeScript are configured. Add a minimal placeholder `src/pages/index.astro` that keeps the site buildable without implementing the Phase 3 homepage yet. Add `src/lib/bookmarks/types.ts` and `src/lib/bookmarks/schema.ts` to define the normalized bookmark record shape shared by sync code and future phases. Seed `src/data/bookmarks.json` as the generated artifact path this phase will own. Expand `.gitignore` for Bun/Astro build outputs while preserving the existing `.dmux/` entry.</action>
  <verify>bun install</verify>
  <acceptance_criteria>
    - `package.json` contains `"sync:bookmarks": "bun run scripts/sync-bookmarks.ts"` and `"test": "bun test"`
    - `src/lib/bookmarks/schema.ts` exports a bookmark schema with `title`, `url`, `description`, `tags`, `done`, `categoryPath`, `slug`, and `sourceRelativePath`
    - `.gitignore` contains `.astro`, `dist`, `node_modules`, and `coverage`
  </acceptance_criteria>
  <done>Project scaffold and shared bookmark contract files exist, and `bun install` completes successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the bookmark sync pipeline and CLI entrypoint</name>
  <files>src/lib/bookmarks/sync.ts, scripts/sync-bookmarks.ts, src/data/bookmarks.json</files>
  <read_first>src/lib/bookmarks/types.ts, src/lib/bookmarks/schema.ts, .planning/phases/01-sync-contract/01-CONTEXT.md, prd.md</read_first>
  <action>Implement `src/lib/bookmarks/sync.ts` as the canonical sync pipeline. It must resolve the source directory from `BOOKMARKS_SOURCE_DIR` or default to `../notes/resources/bookmarks/marks/`, recursively discover markdown files, exclude any path under `articles/`, ignore non-markdown files, parse frontmatter with `gray-matter`, warn-and-skip invalid frontmatter or missing `bookmark_title`/`bookmark_url`, normalize tags to trimmed lowercase arrays, normalize blank or missing `bookmark_done` to `false`, derive `categoryPath` from folder segments under `marks`, set `slug` from the filename stem, preserve `sourceRelativePath`, sort the output deterministically, validate the output against the shared schema, and abort with a clear duplicate slug error before writing output. Implement `scripts/sync-bookmarks.ts` as the CLI wrapper that runs the pipeline, writes `src/data/bookmarks.json`, and prints a concise synced/skipped summary plus warnings.</action>
  <verify>bun run sync:bookmarks</verify>
  <acceptance_criteria>
    - `src/lib/bookmarks/sync.ts` contains logic for `BOOKMARKS_SOURCE_DIR` fallback, `articles/` exclusion, tag normalization, done normalization, category derivation, and duplicate slug detection
    - `scripts/sync-bookmarks.ts` writes `src/data/bookmarks.json` and prints a summary line containing `Synced`
    - `src/data/bookmarks.json` is a non-empty JSON array after running the sync command against the live notes source
  </acceptance_criteria>
  <done>The CLI generates the committed dataset from the current notes corpus and fails correctly on duplicate slugs.</done>
</task>

<task type="auto">
  <name>Task 3: Add fixture coverage for normalization and failure cases</name>
  <files>tests/bookmark-sync.test.ts, tests/fixtures/bookmarks-source/base/root-bookmark.md, tests/fixtures/bookmarks-source/base/tools/test-tool.md, tests/fixtures/bookmarks-source/base/articles/ignored-article.md, tests/fixtures/bookmarks-source/base/articles/ignored-image.png, tests/fixtures/bookmarks-source/base/invalid-frontmatter.md, tests/fixtures/bookmarks-source/base/missing-required.md, tests/fixtures/bookmarks-source/base/nested/blank-values.md, tests/fixtures/bookmarks-source/duplicates/folder-a/same-name.md, tests/fixtures/bookmarks-source/duplicates/folder-b/same-name.md</files>
  <read_first>src/lib/bookmarks/sync.ts, src/lib/bookmarks/schema.ts, .planning/REQUIREMENTS.md</read_first>
  <action>Create fixture directories that model valid bookmarks, excluded `articles/**` content, invalid frontmatter, missing required fields, blank tags and blank `bookmark_done`, and duplicate filename slugs across folders. Add `tests/bookmark-sync.test.ts` using Bun’s test runner to exercise the reusable sync pipeline directly. Assert that successful sync output normalizes tags to lowercase arrays, normalizes blank `bookmark_done` to `false`, derives `categoryPath`, skips invalid/incomplete inputs with warnings, ignores excluded paths and non-markdown files, and throws a duplicate slug error for the duplicate fixture corpus. After the tests pass, rerun the live sync command so the committed JSON artifact matches the real notes corpus.</action>
  <verify>bun test</verify>
  <acceptance_criteria>
    - `tests/bookmark-sync.test.ts` includes assertions for empty tags, blank `bookmark_done`, invalid frontmatter, missing required fields, excluded folders, and duplicate slug detection
    - `bun test` exits with code 0
    - `src/data/bookmarks.json` is regenerated after tests and remains present in the repo
  </acceptance_criteria>
  <done>Required sync edge cases are covered by automated tests, and the generated dataset is up to date.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun test`
- [ ] `bun run sync:bookmarks`
- [ ] `test -s src/data/bookmarks.json`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced outside the expected sync warnings for skipped source files
- Phase 1 requirements are covered by the implemented sync contract and test suite
  </success_criteria>

<output>
After completion, create `.planning/phases/01-sync-contract/01-01-SUMMARY.md`
</output>
